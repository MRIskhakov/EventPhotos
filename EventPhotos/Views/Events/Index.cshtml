@model IEnumerable<EventPhotos.Models.Event>

@{
    ViewBag.Title = "Мероприятия";
}

<h2>Мероприятия</h2>

<p>
    @Html.ActionLink("Добавить мероприятие", "Create")
</p>

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Date)
        </th>
        @*<th>
            @Html.DisplayNameFor(model => model.Place)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Club)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Type)
        </th>*@
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Date)
        </td>
        @*<td>
            @Html.DisplayFor(modelItem => item.Place)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Club)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Type)
        </td>*@
        <td>
            @Html.ActionLink("Просмотреть альбом", "Album", new { id = item.EventID }) |
            @Html.ActionLink("Изменить", "Edit", new { id=item.EventID }) |
            @Html.ActionLink("Детали", "Details", new { id=item.EventID }) |
            @Html.ActionLink("Удалить", "Delete", new { id=item.EventID })
        </td>
    </tr>
}

</table>
